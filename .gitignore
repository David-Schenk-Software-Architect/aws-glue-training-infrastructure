# Secrets
.env

# OpenTofu / Terraform
.terraform/
# .terraform.lock.hcl is committed on purpose (pinned provider hashes for reproducible CI)
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!example.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
